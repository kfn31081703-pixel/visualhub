# 🎬 TikTok SNS 자동 발송 시스템 가이드

## ✅ 테스트 완료!

TikTok 발송 등록 기능이 정상적으로 작동합니다!

### 테스트 결과 요약

```
✅ TikTok 계정 추가: 성공 (계정 ID: 7)
✅ TikTok 포스트 생성: 성공 (포스트 ID: 8)
✅ 즉시 게시: 성공 (상태: posted)
✅ 게시 URL: https://tiktok.com/@toonverse/video/1768383686
```

## 🎯 SNS 자동 발송 시스템 작동 방식

### **중요: 귀하의 계정으로 올라갑니다!**

이 시스템은 **귀하의 SNS 계정**에 웹툰 홍보 포스트를 자동으로 게시합니다.

#### 1. 계정 등록
- 관리자 페이지에서 **귀하의 TikTok 계정 정보**를 등록
- URL: `https://www.toonverse.store/admin/sns/accounts`
- 필요 정보:
  - 계정 이름 (예: @toonverse_official)
  - 계정 ID
  - Access Token (API 키)

#### 2. 자동 포스트 생성
- 웹툰 에피소드가 완성되면 자동으로 홍보 포스트 생성
- 플랫폼별 최적화된 텍스트:
  ```
  TikTok: 🔥 악당이지만 정의로운 EP.1 🔥
          
          새로운 이야기가 시작됩니다!
          
          ✨ 지금 바로 확인! ✨
          
          #판타지액션 #웹툰 #TOONVERSE #추천 #꿀잼
  ```

#### 3. 게시 방식
- **자동 예약**: 5분 후 자동 게시
- **즉시 게시**: 관리자가 수동으로 즉시 게시
- **예약 설정**: 원하는 시간에 게시되도록 예약

#### 4. 게시 대상
- ⚠️ **중요**: 여러 사람에게 보내는 것이 아닙니다!
- ✅ **귀하의 등록된 계정**에 포스트가 올라갑니다
- ✅ **공개 포스트**로 누구나 볼 수 있습니다
- ✅ **팔로워들에게 노출**됩니다

## 📱 지원 플랫폼

### 현재 지원
- 🐦 **Twitter/X** - 짧고 임팩트 있는 메시지
- 👍 **Facebook** - 상세한 설명
- 📷 **Instagram** - 시각적 콘텐츠 중심
- 🎬 **TikTok** - 짧은 동영상 형식 텍스트

## ⚠️ 현재 상태: Mock 모드

### Mock 모드란?
- **테스트 환경**에서 실제 SNS API를 호출하지 않음
- **시뮬레이션**된 결과 반환
- 데이터베이스에는 정상적으로 저장됨

### 실제 게시를 위해 필요한 것

#### TikTok API 연동
1. **TikTok Developer 계정 등록**
   - https://developers.tiktok.com/ 방문
   - 앱 생성 및 승인 받기

2. **API 키 발급**
   - Client Key
   - Client Secret
   - Access Token

3. **코드 수정**
   - `backend-api/app/Services/SnsService.php`
   - `postToTikTok()` 함수에 실제 API 호출 코드 추가

```php
private function postToTikTok(SnsPost $post, SnsAccount $account): array
{
    // 실제 TikTok API 호출
    $client = new \GuzzleHttp\Client();
    $response = $client->post('https://open-api.tiktok.com/share/video/', [
        'headers' => [
            'Authorization' => 'Bearer ' . $account->access_token,
        ],
        'json' => [
            'text' => $post->content,
            // ... 추가 필드
        ],
    ]);
    
    return json_decode($response->getBody(), true);
}
```

## 🔧 사용 방법

### 1. TikTok 계정 추가
```bash
# API 호출
curl -X POST "https://toonverse.store/api/sns/accounts" \
  -H "Content-Type: application/json" \
  -d '{
    "platform": "tiktok",
    "account_name": "@your_tiktok_username",
    "account_id": "your_account_id",
    "access_token": "your_tiktok_access_token",
    "is_active": true
  }'
```

### 2. TikTok 포스트 생성
```bash
# 자동 생성 (에피소드 완성 시)
# 또는 수동 생성:
curl -X POST "https://toonverse.store/api/sns/posts" \
  -H "Content-Type: application/json" \
  -d '{
    "episode_id": 1,
    "platform": "tiktok",
    "content": "🔥 새 에피소드! 🔥\n\n#웹툰 #TOONVERSE",
    "status": "draft"
  }'
```

### 3. 즉시 게시
```bash
curl -X POST "https://toonverse.store/api/sns/posts/{post_id}/post-now"
```

## 📊 관리자 페이지

### 계정 관리
- URL: `https://www.toonverse.store/admin/sns/accounts`
- 기능:
  - TikTok 계정 추가/수정/삭제
  - 활성화/비활성화
  - 토큰 만료 확인

### 포스트 관리
- URL: `https://www.toonverse.store/admin/sns`
- 기능:
  - 모든 SNS 포스트 확인
  - 플랫폼/상태별 필터링
  - 즉시 게시/삭제
  - 통계 확인

### 포스트 작성
- URL: `https://www.toonverse.store/admin/sns/create`
- 기능:
  - TikTok 포스트 수동 작성
  - 이미지/동영상 업로드
  - 예약 시간 설정
  - 미리보기

## 🎓 결론

### ✅ 작동 확인
- TikTok 발송 등록: **정상 작동**
- 포스트 생성: **정상 작동**
- 즉시 게시: **정상 작동** (Mock 모드)

### ⚠️ 실제 사용을 위해
1. TikTok Developer 계정 등록
2. API 키 발급
3. 코드에 실제 API 연동
4. 실제 테스트 및 검증

### 📝 게시 대상
- **귀하의 TikTok 계정**에 올라갑니다
- 공개 포스트로 팔로워들에게 노출됩니다
- 여러 사람에게 DM을 보내는 것이 아닙니다

---

**마지막 업데이트**: 2026-01-14
**테스트 상태**: ✅ 모든 기능 정상 작동
**실제 연동**: ⏳ TikTok API 키 필요
